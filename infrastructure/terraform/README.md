# Infrastructure as Code - Terraform (`infrastructure/terraform`)

## Overview
This directory will contain Terraform modules for managing cloud infrastructure (AWS/GCP, managed PostgreSQL DB, DNS, IAM, secrets).

## Status
* **Milestone 0**: Blueprint directory established. Terraform infrastructure as code is deferred to post-MVP production staging setup.
